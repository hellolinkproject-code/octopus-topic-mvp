import { ArrowRight,CheckCircle2,FileText,PenLine } from 'lucide-react';import { useLocation,useNavigate } from 'react-router-dom';import Layout from '../components/Layout';import { Button,Card } from '../components/ui';import { useApp } from '../context/AppContext'
import { useLanguage } from '../i18n/LanguageContext'
export default function AnswersPage(){const {answers}=useApp();const {t,path,locale}=useLanguage();const navigate=useNavigate();const {state}=useLocation();const graphAnswers=answers.filter(answer=>answer.promptNumber===53);return <Layout><main className="answers-page page-width">{state?.saved?<div className="success-toast"><CheckCircle2/> {t('answers.saved')}</div>:null}<div className="page-heading-row"><div><span>MY WRITING</span><h1>{t('answers.title')}</h1><p>{t('answers.intro')}</p></div><Button onClick={()=>navigate(path('/answers/new'))}><PenLine size={17}/> {t('answers.new')}</Button></div>
 {graphAnswers.length===0?<Card className="empty-state"><div className="empty-icon"><FileText/></div><h2>{t('answers.emptyTitle')}</h2><p>{t('answers.emptyText')}</p><Button onClick={()=>navigate(path('/answers/new'))}>{t('answers.first')} <ArrowRight size={17}/></Button></Card>:<div className="answer-list">{graphAnswers.map(answer=>{const detailPath=path(`/answers/${answer.id}`);return <Card className="answer-item" key={answer.id} onClick={()=>navigate(detailPath)} tabIndex="0" onKeyDown={e=>e.key==='Enter'&&navigate(detailPath)}><div className="answer-number">53</div><div className="answer-copy"><span>{new Intl.DateTimeFormat(locale,{year:'numeric',month:'long',day:'numeric'}).format(new Date(answer.createdAt))}</span><h2>{answer.title}</h2><p>{answer.content.slice(0,110)}{answer.content.length>110?'…':''}</p><div><span>{answer.characterCount}{t('common.characters')}</span><span>{t('answers.graph')}</span></div></div><ArrowRight className="answer-arrow"/></Card>})}</div>}</main></Layout>}
